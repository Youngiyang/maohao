<<<<<<< HEAD
# PostgreSQL. Versions 8.2 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On OS X with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
# On OS X with MacPorts:
#   gem install pg -- --with-pg-config=/opt/local/lib/postgresql84/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem 'pg'
#
default: &default
  adapter: postgis
  encoding: unicode
  postgis_extension: postgis
  schema_search_path: public
  su_username: postgres
  su_password: postgres1234
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: 5

development:
  <<: *default
  database: playmall_development
  username: postgres
  password: postgres1234
test:
  <<: *default
  database: playmall_test
  username: postgres
  password: postgres1234

production:
  <<: *default
  host: <%= ENV['MALL_DATABASE_HOST'] %>
  su_username: postgres
  su_password: <%= ENV['SU_DATABASE_PASSWORD'] %>
  database: playmall_production
  username: playmall
  password: <%= ENV['MALL_DATABASE_PASSWORD'] %>
=======
# PostgreSQL. Versions 8.2 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On OS X with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
# On OS X with MacPorts:
#   gem install pg -- --with-pg-config=/opt/local/lib/postgresql84/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem 'pg'
#
default: &default
  adapter: postgis
  encoding: unicode
  postgis_extension: postgis
  schema_search_path: public
  su_username: postgres
  su_password: postgres1234
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: 5

development:
  <<: *default
  database: playmall_development
  username: postgres
  password: postgres1234
test:
  <<: *default
  database: playmall_test
  username: postgres
  password: postgres1234

production:
  <<: *default
  host: <%= ENV['PLAYMALL_DATABASE_HOST'] %>
  su_username: postgres
  su_password: <%= ENV['SU_DATABASE_PASSWORD'] %>
  database: playmall_production
  username: playmall
  password: <%= ENV['PLAYMALL_DATABASE_PASSWORD'] %>
>>>>>>> f9ece0be89728d55a2cf606100b8bf704bfba3f8
